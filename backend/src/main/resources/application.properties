spring.application.name=EncryptedFileSharing

spring.datasource.url=${SPRING_DATASOURCE_URL}

spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT
# 1 day in milliseconds
app.jwt.expiration-ms=86400000
# Use a long random string (min 32 chars) ? do NOT commit this to VCS
app.jwt.secret=ReplaceWithAStrongRandomSecretKeyOfAtLeast32Chars



# Mail (example using Gmail SMTP: set app password or proper SMTP for prod)
# Mail (example using Gmail SMTP: set app password or proper SMTP for prod)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sathishsk2285@gmail.com
spring.mail.password=cqlmfcwejdneense
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true